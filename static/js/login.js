// The login card's behavior (sign in / register / Sign in with Google) lives
// in app.js — see the "Login / Register / Sign in with Google" section there.
// app.js loads after this file, so any duplicate function definitions here
// would just be the ones that lose; this file is kept only because
// templates/index.html still lists it in its <script> tags.
